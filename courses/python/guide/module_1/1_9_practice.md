# 🎯 1.9 Практика: минимальный Python

Обязательная тренировка перед pandas. Цель — автоматизм в базовых действиях.

**Как выполнять:**

- пиши код в Jupyter, в своём ноутбуке (он останется в твоей базе знаний)
- 20–30 минут в день, повторяй те же задачи 2–3 дня подряд
- решай **сам, без LLM** — на собеседовании его не будет
- решение открывай только после своей попытки
- критерий готовности: уровни 1–2 решаются легко, без долгих размышлений

---

## Уровень 1 — лёгкий

**1. Создание переменных.** Создай переменные: `age` (число), `name` (строка), `height` (float), `is_active` (bool). Выведи их на экран.

**2. Типы данных.** Выведи тип каждой переменной с помощью `type()`.

**3. Арифметика.** Даны `a = 7`, `b = 3`. Вычисли: сумму, разницу, произведение, целочисленное деление, остаток, степень `a**b`.

**4. Индексация и срезы.** Дан `nums = [5, 10, 15, 20, 25]`. Получи: первый элемент, последний, срез с 1 по 3 индекс, длину списка.

**5. Изменение списка.** Добавь 30 в конец, удали число 10, получи перевёрнутую копию списка через срез.

**6. Словари.** Создай `user = {"name": "Alex", "age": 25, "city": "Moscow"}`. Измени age на 26, добавь ключ `"is_active": True`, удали ключ `"city"`.

**7. Множества.** Дан `vals = [1, 2, 2, 3, 4, 4, 5]`. Получи множество уникальных значений.

**8. Условия.** Если `age >= 18` — выведи `"adult"`, иначе `"minor"`.

**9. Цикл for.** Выведи все элементы списка `names = ["Bob", "Ann", "Tom"]`.

**10. Функция.** Напиши `greet(name)`, возвращающую строку `"Hello, <name>"`.

**11. Строки.** Дана `s = "  Data Analyst  "`. Убери пробелы по краям, приведи к нижнему регистру, проверь через `in`, есть ли подстрока `"data"`.

**12. f-string.** Даны `visitors = 1000`, `buyers = 45`. Выведи строку `"Конверсия: 4.5%"` через f-string с округлением до 1 знака.

<details>
<summary>✅ Решения уровня 1</summary>

```python
# 1. Создание переменных
age = 25
name = "Alex"
height = 1.82
is_active = True
print(age, name, height, is_active)

# 2. Типы данных
print(type(age))
print(type(name))
print(type(height))
print(type(is_active))

# 3. Арифметика
a, b = 7, 3
print(a + b)    # сумма
print(a - b)    # разница
print(a * b)    # произведение
print(a // b)   # целочисленное деление
print(a % b)    # остаток
print(a ** b)   # степень

# 4. Индексация и срезы
nums = [5, 10, 15, 20, 25]
print(nums[0])      # первый
print(nums[-1])     # последний
print(nums[1:4])    # срез
print(len(nums))    # длина

# 5. Изменение списка
nums.append(30)
nums.remove(10)
print(nums[::-1])   # перевёрнутая копия — срез с шагом -1 (раздел 1.3)

# 6. Словари
user = {"name": "Alex", "age": 25, "city": "Moscow"}
user["age"] = 26
user["is_active"] = True
del user["city"]
print(user)

# 7. Множества
vals = [1, 2, 2, 3, 4, 4, 5]
unique_vals = set(vals)
print(unique_vals)

# 8. Условия
if age >= 18:
    print("adult")
else:
    print("minor")

# 9. Цикл for
names = ["Bob", "Ann", "Tom"]
for n in names:
    print(n)

# 10. Функция
def greet(name):
    return "Hello, " + name       # конкатенация строк — раздел 1.3b
    # эквивалент через f-string: return f"Hello, {name}"

print(greet("Alex"))

# 11. Строки (раздел 1.3b)
s = "  Data Analyst  "
s = s.strip().lower()
print(s)                  # → "data analyst"
print("data" in s)        # → True

# 12. f-string (раздел 1.3b)
visitors, buyers = 1000, 45
print(f"Конверсия: {buyers / visitors * 100:.1f}%")   # → Конверсия: 4.5%
```

</details>

---

## Уровень 2 — средний

**1. Квадраты.** Дан `[1, 2, 3, 4, 5]`. Создай новый список квадратов чисел.

**2. Фильтрация.** Из `[3, 12, 5, 18, 1, 20]` оставь только элементы больше 10.

**3. Минимум и максимум вручную.** Из `nums = [14, 2, 9, 27, 1]` найди минимум и максимум **без** `min()`/`max()`.

**4. Разворот вручную.** Получи обратный список без `.reverse()` и срезов.

**5. Частоты.** Из `["a", "b", "a", "c", "b", "a"]` создай словарь частот: `{"a": 3, "b": 2, "c": 1}`.

**6. Фильтрация словаря.** Из `{"a": 5, "b": 15, "c": 2, "d": 20}` выбери только пары со значением больше 10.

**7. Перебор через range(len()).** Для `[10, 20, 30]` выведи `index: 0 -> 10` и так далее.

**8. Comprehensions.** Перепиши задачи 1 (квадраты) и 2 (фильтрация) одной строкой каждую.

**9. Что выведет?** Ответь, не запуская код, потом проверь в Jupyter:

```python
a = [1, 2, 3]
b = a
b.append(4)
print(a)
```

<details>
<summary>✅ Решения уровня 2</summary>

```python
# 1. Квадраты
nums = [1, 2, 3, 4, 5]
squares = []
for n in nums:
    squares.append(n**2)
print(squares)

# 2. Фильтрация
nums = [3, 12, 5, 18, 1, 20]
filtered = []
for n in nums:
    if n > 10:
        filtered.append(n)
print(filtered)

# 3. Минимум и максимум вручную
nums = [14, 2, 9, 27, 1]
mn, mx = nums[0], nums[0]
for n in nums:
    if n < mn:
        mn = n
    if n > mx:
        mx = n
print(mn, mx)

# 4. Разворот списка вручную
nums = [1, 2, 3, 4, 5]
rev = []
for i in range(len(nums) - 1, -1, -1):
    rev.append(nums[i])
print(rev)

# 5. Частоты
data = ["a", "b", "a", "c", "b", "a"]
freq = {}
for x in data:
    if x not in freq:
        freq[x] = 1
    else:
        freq[x] += 1
print(freq)

# 6. Фильтрация словаря
d = {"a": 5, "b": 15, "c": 2, "d": 20}
filtered = {}
for k, v in d.items():
    if v > 10:
        filtered[k] = v
print(filtered)

# 7. range(len())
nums = [10, 20, 30]
for i in range(len(nums)):
    print("index:", i, "->", nums[i])

# 8. Comprehensions (раздел 1.7b)
squares = [n**2 for n in [1, 2, 3, 4, 5]]
filtered = [n for n in [3, 12, 5, 18, 1, 20] if n > 10]
print(squares, filtered)

# 9. Что выведет?
# Ответ: [1, 2, 3, 4]. b = a — не копия, а вторая ссылка
# на тот же список (алиасинг, раздел 1.2). Чтобы оригинал
# не менялся — b = a.copy()
```

</details>

---

## Уровень 3 — сложный (опциональный)

Если хочешь дополнительный вызов. Эти паттерны вернутся в модуле алгоритмов (расширенный тариф).

**1. Чётные числа.** Из `[1, 2, 3, 4, 5, 6, 7, 8]` получи только чётные.

**2. Второй максимум.** `[10, 5, 8, 20, 3]` → `10`.

**3. Количество пар одинаковых чисел.** `[1, 2, 1, 2, 1, 5]` → 2 пары (пара единиц + пара двоек).

**4. Словарь «значение → индекс».** `[10, 20, 30]` → `{10: 0, 20: 1, 30: 2}`.

**5. Порог.** Удали все элементы меньше порога `t = 10` из `[3, 12, 7, 18, 1, 20]`.

**6. Попарное объединение.** `a = [1, 2, 3]`, `b = [4, 5, 6]` → `[(1, 4), (2, 5), (3, 6)]`.

**7. Топ-2 частоты.** Из `["a", "b", "a", "c", "b", "a"]` получи два самых частых элемента с их количеством: `[('a', 3), ('b', 2)]`.

**8. Палиндром.** Напиши функцию `is_palindrome(s)`, которая игнорирует регистр и пробелы. Проверь на `"А роза упала на лапу Азора"` (→ True) и `"Python"` (→ False).

<details>
<summary>✅ Решения уровня 3 (с построчным разбором)</summary>

```python
# 1. Чётные числа
nums = [1, 2, 3, 4, 5, 6, 7, 8]
evens = []                       # пустой список для чётных

for n in nums:                   # перебираем каждый элемент
    if n % 2 == 0:               # делится на 2 без остатка?
        evens.append(n)          # да — добавляем

print(evens)

# 2. Второй максимум
nums = [10, 5, 8, 20, 3]

mx = nums[0]                     # ищем реальный максимум
for n in nums:
    if n > mx:
        mx = n

second = None                    # переменная для второго максимума
for n in nums:
    if n != mx:                  # пропускаем сам максимум
        if second is None or n > second:   # is None — раздел 1.6
            second = n           # обновляем, если больше текущего

print(second)

# 3. Количество пар одинаковых чисел
arr = [1, 2, 1, 2, 1, 5]
freq = {}                        # число → сколько раз встретилось

for x in arr:
    if x not in freq:
        freq[x] = 1
    else:
        freq[x] += 1

pairs = 0
for v in freq.values():
    pairs += v // 2              # каждая пара — 2 одинаковых элемента

print(pairs)

# 4. Словарь "значение → индекс"
lst = [10, 20, 30]
d = {}

for i, v in enumerate(lst):      # enumerate даёт (индекс, значение)
    d[v] = i

print(d)

# 5. Порог
nums = [3, 12, 7, 18, 1, 20]
t = 10
res = []

for n in nums:
    if n >= t:                   # оставляем только >= порога
        res.append(n)

print(res)

# 6. Попарное объединение
a = [1, 2, 3]
b = [4, 5, 6]
pairs = []

for i in range(len(a)):          # перебираем индексы
    pairs.append((a[i], b[i]))   # пара из элементов с одним индексом

print(pairs)
# короче — через zip (раздел 1.7): list(zip(a, b))

# 7. Топ-2 частоты
data = ["a", "b", "a", "c", "b", "a"]
freq = {}                        # частоты — паттерн из 1.4

for x in data:
    if x not in freq:
        freq[x] = 1
    else:
        freq[x] += 1

top = sorted(freq.items(),       # сортировка по значению — раздел 1.8b
             key=lambda x: x[1],
             reverse=True)
print(top[:2])                   # срезом берём первые два

# 8. Палиндром
def is_palindrome(s):
    clean = s.lower().replace(" ", "")   # методы строк — раздел 1.3b
    return clean == clean[::-1]          # разворот срезом — раздел 1.3

print(is_palindrome("А роза упала на лапу Азора"))   # → True
print(is_palindrome("Python"))                       # → False
```

</details>

---

## Чек-лист завершения модуля

- [ ] Уровни 1–2 решаю без подсказок и без LLM
- [ ] Понимаю разницу mutable / immutable
- [ ] Могу объяснить, когда list, когда dict, когда set
- [ ] Знаю паттерн «частоты через словарь»
- [ ] Мои функции всегда возвращают результат через return

Всё отмечено → переходи к [модулю 2: Pandas](../module_2/00_index.md).
