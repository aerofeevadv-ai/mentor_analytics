# 4.7 Мок-собеседование 1 — базовый pandas

**Формат:** 40 минут, 4 задачи. Базовые операции: groupby, merge, transform, даты.

---

## Правила

📌 **Обязательно:**
- Запусти таймер на 40 минут до начала
- Решай вслух — проговаривай план, объясняй каждый шаг
- LLM и документацию не открывать
- После каждой операции проверяй результат: `print(df.head())`, `print(df.shape)`

⚠️ **Задачи самодостаточны** — все данные прямо в коде, ничего загружать не нужно.

📊 **После прохождения** — проверь себя в [4.9 Разбор мок-собеседований](4_9_mock_review.md).

---

## Задача 1. Дашборд по городам

**Условие.** Сгруппируй заказы по городу. Для каждого города посчитай: число заказов, число уникальных покупателей, суммарную выручку, средний чек. Отсортируй по выручке убыванию.

```python
import pandas as pd

orders = pd.DataFrame({
    "order_id": [1, 2, 3, 4, 5, 6],
    "city":     ["Moscow", "SPB", "Moscow", "Kazan", "SPB", "Moscow"],
    "user_id":  [1, 2, 1, 3, 4, 2],
    "amount":   [15000, 2500, 8000, 500, 3000, 700]
})
```

---

## Задача 2. Обогащение сегментом — с ловушкой

**Условие.** Прилепи к заказам сегмент пользователя. Убедись что число строк после merge равно числу строк до.

```python
import pandas as pd

orders = pd.DataFrame({
    "order_id": [1, 2, 3, 4],
    "user_id":  [10, 20, 10, 30],
    "amount":   [500, 1200, 800, 300]
})
users = pd.DataFrame({
    "user_id": [10, 10, 20, 30],    # обрати внимание!
    "segment": ["vip", "vip", "regular", "new"]
})
```

---

## Задача 3. Доля канала в общем бюджете

**Условие.** Для каждой строки добавь колонку `channel_total` — суммарный бюджет канала. И `share_pct` — доля данной кампании в бюджете своего канала. Использовать только `transform`, без merge.

```python
import pandas as pd

df = pd.DataFrame({
    "channel":     ["email", "email", "social", "social", "context"],
    "campaign_id": [1, 2, 3, 4, 5],
    "spend":       [50000, 30000, 80000, 20000, 40000]
})
```

---

## Задача 4. Динамика по месяцам

**Условие.** Оставь только события за 2026 год. Сгруппируй по месяцу, посчитай число уникальных пользователей и суммарную выручку.

```python
import pandas as pd

df = pd.DataFrame({
    "event_id":    range(1, 7),
    "user_id":     [1, 1, 2, 2, 3, 3],
    "event_date":  pd.to_datetime(["2026-01-15", "2026-02-10", "2026-01-20",
                                    "2026-02-25", "2026-03-05", "2025-12-30"]),
    "revenue":     [1000, 2000, 1500, 500, 800, 300]
})
```

---

⏱️ Время вышло? Переходи к [разбору →](4_9_mock_review.md)
